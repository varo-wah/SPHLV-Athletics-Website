name: Verify and deploy production

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

permissions:
  contents: read

concurrency:
  group: firebase-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Verify both channels and deploy Firebase
    runs-on: ubuntu-latest
    environment: production
    env:
      VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
    steps:
      - name: Check out main
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Refresh published Google Sheets cache
        id: sheets
        env:
          DEPLOYED_CACHE_URL: https://sphlv-athletics.web.app/data/sheets-cache.json
        run: npm run sync:sheets

      - name: Type-check
        if: github.event_name != 'schedule' || steps.sheets.outputs.changed == 'true'
        run: npm run lint

      - name: Test
        if: github.event_name != 'schedule' || steps.sheets.outputs.changed == 'true'
        run: npm test

      - name: Verify prototype build
        if: github.event_name != 'schedule' || steps.sheets.outputs.changed == 'true'
        run: npm run build:prototype

      - name: Build restricted production release
        if: github.event_name != 'schedule' || steps.sheets.outputs.changed == 'true'
        run: npm run build:production

      - name: Authenticate to Google Cloud
        if: github.event_name != 'schedule' || steps.sheets.outputs.changed == 'true'
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SPHLV_ATHLETICS }}

      - name: Deploy Firebase Hosting and Firestore rules
        if: github.event_name != 'schedule' || steps.sheets.outputs.changed == 'true'
        run: npm run deploy:firebase:ci
